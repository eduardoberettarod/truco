import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    input: {
        flex: 1,
        textAlign: 'center',
        borderRadius: 10,
        borderColor: 'black',
        height: 120,
        borderWidth: 1,
        backgroundColor: '#f2f2f2'
    }
})